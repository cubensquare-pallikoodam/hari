apiVersion: apps/v1
kind: Deployment
metadata:
  name: hari-flask-app
  namespace: batch2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hari-flask-app
  template:
    metadata:
      labels:
        app: hari-flask-app


    spec:
      containers:
        - name: flask-app-container
          image: 915551958967.dkr.ecr.ap-south-1.amazonaws.com/flask_container:hariflask 
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
              protocol: TCP
